# Database Configuration
DATABASE_URI=postgresql://postgres:postgres@localhost/erp_system

# Secret Key (generate a strong key in production)
SECRET_KEY=dev_key_only_for_development

# Flask Configuration
FLASK_APP=run.py
FLASK_ENV=development
FLASK_DEBUG=1

# Server Configuration
PORT=5000
HOST=0.0.0.0